import { generateRandom, sum } from 'module';

console.log(generateRandom());    //logs a random number
console.log(sum(1, 2)); // 3

import { generateSub, sub } from 'module';

console.log(generateSub());    //logs a random number
console.log(sub(2, 1)); // 1

import { generateMul, mul } from 'module';

console.log(generateMul());    //logs a random number
console.log(mul(1, 2)); // 2

import { generateDiv, div } from 'module';

console.log(generateDiv());    //logs a random number
console.log(div(4, 2)); // 2
